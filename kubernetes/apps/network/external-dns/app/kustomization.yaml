---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./source.yaml
  - ./helmrelease.yaml
  # - ./dnsendpoint.yaml # Not needed if external-dns annotations work
  - ./grafanadashboard.yaml
  - ./secret.sops.yaml
