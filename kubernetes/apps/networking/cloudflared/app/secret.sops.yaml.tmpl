---
# yamllint disable
apiVersion: v1
metadata:
  name: cloudflared
  namespace: networking
  annotations:
    reloader.stakater.com/match: "true"
kind: Secret
data:
  credentials.json: ${SECRET_CLOUDFLARE_TUNNEL_CREDS}
