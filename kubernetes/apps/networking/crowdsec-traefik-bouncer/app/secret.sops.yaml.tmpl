# yamllint disable
apiVersion: v1
kind: Secret
metadata:
    name: crowdsec-traefik-bouncer-secret
    namespace: networking
    annotations:
        reloader.stakater.com/match: "true"
stringData:
    # generated from crowdsec lapi on k8s with
    # cscli bouncers add traefik-ingress
    crowdsec_bouncer_api_key: ""
