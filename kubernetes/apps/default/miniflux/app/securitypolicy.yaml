---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: &name miniflux-oidc
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: &route miniflux
  oidc:
    provider:
      issuer: "https://auth.${SECRET_DOMAIN}"
    clientID: *route
    clientSecret:
      name: *name
    redirectURL: "https://miniflux.${SECRET_DOMAIN}/oauth2/callback"
    logoutPath: "/logout"
