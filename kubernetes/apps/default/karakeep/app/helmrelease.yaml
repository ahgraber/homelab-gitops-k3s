---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app karakeep
spec:
  chartRef:
    kind: OCIRepository
    name: *app
  interval: 1h
  values:
    # NOTE: if db migrations fail, disable for the migration, then reenable
    defaultPodOptions:
      securityContext:
        runAsUser: 568
        runAsGroup: 568
        runAsNonRoot: true
        fsGroup: 568
        fsGroupChangePolicy: "OnRootMismatch"
    controllers:
      karakeep:
        annotations:
          reloader.stakater.com/search: "true"
        initContainers:
          wait-for-meilisearch:
            image:
              repository: ghcr.io/containerd/busybox
              tag: 1.36
            command:
              - sh
              - -c
              - |
                until wget -q --spider http://karakeep-meilisearch.default.svc.cluster.local:7700/health; do
                  echo "Waiting for meilisearch..."
                  sleep 2
                done
                echo "Meilisearch is ready!"
        containers:
          app:
            image:
              repository: ghcr.io/karakeep-app/karakeep
              tag: 0.31.0
            env:
              BROWSER_WEB_URL: http://karakeep-chrome.default.svc.cluster.local:9222
              LOG_LEVEL: "notice" # "debug"
              DISABLE_SIGNUPS: true
              DISABLE_NEW_RELEASE_CHECK: true
              CRAWLER_NUM_WORKERS: 2
              CRAWLER_DOMAIN_RATE_LIMIT_WINDOW_MS: 15000
              CRAWLER_DOMAIN_RATE_LIMIT_REQUESTS: 2
              CRAWLER_JOB_TIMEOUT_SEC: 120
              CRAWLER_ENABLE_ADBLOCKER: true
              CRAWLER_DOWNLOAD_BANNER_IMAGE: true
              CRAWLER_STORE_SCREENSHOT: true
              CRAWLER_FULL_PAGE_SCREENSHOT: true
              CRAWLER_SCREENSHOT_TIMEOUT_SEC: 120
              CRAWLER_ALLOWED_INTERNAL_HOSTNAMES: ".local,.${SECRET_DOMAIN}"
              DATA_DIR: /data
              MAX_ASSET_SIZE_MB: 128
              ASSET_PREPROCESSING_NUM_WORKERS: 2
              ASSET_PREPROCESSING_JOB_TIMEOUT_SEC: 120
              SEARCH_NUM_WORKERS: 4
              SEARCH_JOB_TIMEOUT_SEC: 60
              MEILI_ADDR: http://karakeep-meilisearch.default.svc.cluster.local:7700
              NEXTAUTH_URL: https://karakeep.${SECRET_DOMAIN}
              # OpenAI Config
              OPENAI_BASE_URL: https://openrouter.ai/api/v1
              INFERENCE_TEXT_MODEL: openai/gpt-5-mini
              INFERENCE_IMAGE_MODEL: openai/gpt-5-mini
              EMBEDDING_TEXT_MODEL: openai/text-embedding-3-small
              INFERENCE_CONTEXT_LENGTH: "8192"
              INFERENCE_USE_MAX_COMPLETION_TOKENS: true
              # OIDC
              OAUTH_WELLKNOWN_URL: https://auth.${SECRET_DOMAIN}/.well-known/openid-configuration
              OAUTH_CLIENT_SECRET:
                valueFrom:
                  secretKeyRef:
                    name: karakeep-oidc
                    key: client-secret
              OAUTH_CLIENT_ID: karakeep
              OAUTH_SCOPE: "openid profile email"
              OAUTH_PROVIDER_NAME: authelia
              OAUTH_ALLOW_DANGEROUS_EMAIL_ACCOUNT_LINKING: true
            envFrom:
              - secretRef:
                  name: *app
            probes:
              startup:
                enabled: true
                spec:
                  initialDelaySeconds: 30 # Wait 30 seconds before first check
                  periodSeconds: 10
                  failureThreshold: 30 # Total wait time: 30 + (10 * 30) = 330 seconds
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources:
              requests:
                cpu: 250m
                memory: 1Gi
              limits:
                memory: 4Gi

      chrome:
        containers:
          app:
            image:
              repository: gcr.io/zenika-hub/alpine-chrome
              tag: 124
            command:
              - chromium-browser
            args:
              - --headless
              - --no-sandbox
              - --disable-gpu
              - --disable-background-timer-throttling
              - --disable-extensions
              - --disable-default-apps
              - --no-first-run
              - --no-default-browser-check
              - --mute-audio
              - --remote-debugging-address=0.0.0.0
              - --remote-debugging-port=9222
              - --hide-scrollbars
            probes:
              liveness:
                enabled: true
                spec:
                  httpGet:
                    path: /json/version
                    port: 9222
                  initialDelaySeconds: 10
                  periodSeconds: 10
                  timeoutSeconds: 5
                  failureThreshold: 6
              readiness:
                enabled: true
                spec:
                  httpGet:
                    path: /json/version
                    port: 9222
                  initialDelaySeconds: 5
                  periodSeconds: 5
                  timeoutSeconds: 3
                  failureThreshold: 6
            securityContext:
              capabilities:
                drop:
                  - ALL
                add:
                  - SYS_NICE
            resources:
              requests:
                cpu: 100m
                memory: 1Gi
              limits:
                memory: 4Gi

      meilisearch:
        pod:
          affinity:
            podAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                - labelSelector:
                    matchExpressions:
                      - key: app.kubernetes.io/name
                        operator: In
                        values:
                          - karakeep
                  topologyKey: kubernetes.io/hostname
        containers:
          app:
            image:
              repository: docker.io/getmeili/meilisearch
              tag: v1.35.1
            args:
              - /bin/meilisearch
              - --experimental-dumpless-upgrade
            env:
              MEILI_NO_ANALYTICS: true
              MEILI_MASTER_KEY:
                valueFrom:
                  secretKeyRef:
                    name: *app
                    key: MEILI_MASTER_KEY
            resources:
              requests:
                cpu: 100m
                memory: 3Gi
              limits:
                memory: 8Gi

    service:
      app:
        controller: *app
        ports:
          http:
            port: 3000
      chrome:
        controller: chrome
        ports:
          http:
            port: 9222
      meilisearch:
        controller: meilisearch
        ports:
          http:
            port: 7700

    route:
      app:
        hostnames:
          - &host "{{ .Release.Name }}.${SECRET_DOMAIN}"
        parentRefs:
          - name: envoy-internal
            namespace: network
            sectionName: https
        rules:
          - matches:
              - path:
                  type: PathPrefix
                  value: /
            backendRefs:
              - identifier: app
                port: 3000

    persistence:
      data:
        existingClaim: *app
        advancedMounts:
          karakeep:
            app:
              - path: /data
                subPath: karakeep
          meilisearch:
            app:
              - path: /meili_data
                subPath: meilisearch
      shm:
        type: emptyDir
        medium: Memory
        sizeLimit: 1Gi
        advancedMounts:
          chrome:
            app:
              - path: /dev/shm
      # This is a workaround to create cache dirs (nonroot cannot mkdir)
      cache:
        type: emptyDir
        advancedMounts:
          karakeep:
            app:
              - path: /.cache
      appcache:
        type: emptyDir
        advancedMounts:
          karakeep:
            app:
              - path: /app/apps/web/.next/cache
