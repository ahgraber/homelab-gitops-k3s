---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mlflow
  namespace: datasci
spec:
  storageClassName: ceph-block
  accessModes:
    - ReadWriteOnce
  # storageClassName: ceph-fs
  # accessModes:
  #   - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
