---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: cert-manager-source
  namespace: flux-system
spec:
  interval: 30m
  url: https://github.com/cert-manager/cert-manager.git
  ref:
    # renovate: registryUrl=https://github.com/cert-manager/cert-manager
    tag: v1.8.0
  ignore: |
    # exclude all
    /*
    # include crds dir
    !/deploy/crds/*.yaml
---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: cert-manager-crds
  namespace: flux-system
spec:
  interval: 15m
  prune: false
  # wait: true
  sourceRef:
    kind: GitRepository
    name: cert-manager-source
