---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # - _namespaces/
  - cert-manager/
  - democratic-csi/
  - flux-system/
  - kube-system/
  # - kube-vip/  # managed in ansible k3s install
  - kyverno/
  - metallb-system/
  # - rook-ceph/
  - tigera-operator/ # may conflict from version managed in ansible k3s install
  ### overwrite local-path StorageClass to set as NOT default
  - storage-class-local.yaml
