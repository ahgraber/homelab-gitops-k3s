---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - _namespaces/
  - cert-manager/
  - db-operators/
  - democratic-csi/
  - flux-system/
  - kube-system/
  # - kube-vip/  # managed in ansible k3s install
  - kyverno/
  - metallb-system/
  - rook-ceph/
  - tigera-operator/ # may conflict from version managed in ansible k3s install
  - storage-class-local.yaml # overwrite local-path StorageClass to set as NOT default
