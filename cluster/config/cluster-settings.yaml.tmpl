---
# yamllint disable
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-settings
  namespace: flux-system
  annotations:
    reloader.stakater.com/match: "true"
data:
  TIMEZONE: "${TIMEZONE}"
  LB_INGRESS: ${SETTINGS_LB_INGRESS}
  LB_DEFAULT_RANGE: ${SETTINGS_LB_DEFAULT_RANGE}
