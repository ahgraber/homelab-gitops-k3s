---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-settings
  namespace: flux-system
data:
  METALLB_LB_RANGE: ${SETTINGS_METALLB_LB_RANGE}
  METALLB_FRONTEND: ${SETTINGS_METALLB_FRONTEND}
  METALLB_GUI: ${SETTINGS_METALLB_GUI}
  METALLB_MARIADB: ${SETTINGS_METALLB_MARIADB}
  METALLB_POSTGRES: ${SETTINGS_METALLB_POSTGRES}
