---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: crunchdata-pgo-charts-git
  namespace: flux-system
spec:
  interval: 10m
  url: https://github.com/CrunchyData/postgres-operator-examples
  ref:
    branch: main
  ignore: |
    # exclude all
    /*
    # include charts directory
    !/helm/
