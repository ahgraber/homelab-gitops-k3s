---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # core/ services are configured prior to monitoring/, so manage with monitoring/
  - cert-manager.yaml
  - etcd.yaml
  # monitoring/ services should be managed with monitoring/
  - loki.yaml
  - node-exporter.yaml
  # app/ services are configured after monitoring/, so manage with the application
  - crunchydata.yaml
  - kured.yaml
  - traefik.yaml
  - velero.yaml
  # - upsc.yaml
