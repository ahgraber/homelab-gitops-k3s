---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kube-prometheus-stack
  namespace: monitoring
spec:
  interval: 6h
  maxHistory: 2
  timeout: 20m
  chart:
    spec:
      # renovate: registryUrl=https://prometheus-community.github.io/helm-charts
      chart: kube-prometheus-stack
      version: 34.10.0
      sourceRef:
        kind: HelmRepository
        name: prometheus-community-charts
        namespace: flux-system
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3

  values:
    podAnnotations:
      annotations:
        configmap.reloader.stakater.com/reload: "kps-values-general,kps-values-prometheus,kps-values-alertmanager"
    fullnameOverride: prometheus

    defaultRules:
      create: true

  valuesFrom:
    - kind: ConfigMap
      name: kps-values-general
    - kind: ConfigMap
      name: kps-values-prometheus
    - kind: ConfigMap
      name: kps-values-alertmanager
