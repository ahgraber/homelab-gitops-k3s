---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: dashboard-admin
  namespace: monitoring
---
kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: dashboard-admin
  namespace: monitoring
roleRef:
  kind: ClusterRole
  name: cluster-admin
  apiGroup: ""
subjects:
  # for token login
  - kind: ServiceAccount
    name: dashboard-admin
    namespace: monitoring
  # for SSO login with OIDC
  # - kind: Group
  #   name: /${SECRET_DOMAIN/.com/-}admin
  - kind: Group
    name: admin
# ---
# kind: ClusterRole
# apiVersion: rbac.authorization.k8s.io/v1alpha1
# metadata:
#   name: admin-role
# rules:
# - apiGroups: ["*"]
#   resources: ["*"]
#   verbs: ["*"]
#   nonResourceURLs: ["*"]
