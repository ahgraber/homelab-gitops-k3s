---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  ### loading in /apps ensures rules are installed after prometheus is available
  ## core/ services
  - cert-manager.yaml
  # - rook-ceph.yaml (use direct link instead (below))
  - https://raw.githubusercontent.com/rook/rook/v1.9.6/deploy/examples/monitoring/localrules.yaml
  ## monitoring/ services
  - etcd.yaml
  # - loki.yaml
  - node-exporter.yaml
  ## app/ services
  - crunchydata.yaml
  - kured.yaml
  - traefik.yaml
  - velero.yaml
  # - upsc.yaml

# patches:
patchesStrategicMerge:
  - patch-rook-ceph.yaml
