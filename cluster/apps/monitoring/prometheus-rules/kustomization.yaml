---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./cert-manager.yaml
  - ./cloudnative-pg.yaml
  - ./etcd.yaml
  - ./flux.yaml
  - ./network-ups-tools.yaml
  - ./node-exporter.yaml
  - https://raw.githubusercontent.com/rook/rook/v1.10.6/deploy/examples/monitoring/localrules.yaml

# patchesJson6902:
patches:
  - path: patch-rook-ceph.yaml
    target:
      group: monitoring.coreos.com
      version: v1
      kind: PrometheusRule
      name: prometheus-ceph-rules
      namespace: rook-ceph
