# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: secret-oauth2proxy
  namespace: security
stringData:
  client-id: k8s
  client-secret: ${SECRET_OAUTH2PROXY_SECRET}
  cookie-secret: ${SECRET_OAUTH2PROXY_COOKIE}