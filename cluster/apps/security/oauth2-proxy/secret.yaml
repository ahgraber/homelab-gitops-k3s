---
# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: secret-oauth2proxy
  namespace: security
stringData:
  client-id: ${SECRET_DASHBOARD_CLIENT_ID}
  client-secret: ${SECRET_DASHBOARD_CLIENT_SECRET}
  cookie-secret: ${SECRET_OAUTH_COOKIE_SECRET}
