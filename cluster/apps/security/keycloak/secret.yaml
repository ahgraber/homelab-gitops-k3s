# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: secret-keycloak
  namespace: security
stringData:
  admin-password: ${SECRET_DEFAULT_PASSWORD}
  management-password: ${SECRET_DEFAULT_PASSWORD}
  database-password: ${SECRET_DB_ROOT_PWD}
  # tls-keystore-password: myTlsKeystorePasswordKey
  # tls-truestore-password: myTlsTruestorePasswordKey