---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: route-keycloak-admin
  namespace: networking
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`keycloak.${SECRET_DOMAIN}`)
      kind: Rule
      middlewares:
        - name: rfc1918
        # - name: authentik
        # - name: proxy-headers
      services:
        - name: keycloak
          namespace: security
          port: 8080

  # Use the secret generated by cert-manager
  tls:
    secretName: "${SECRET_DOMAIN/./-}-tls"
    # secretName: '${SECRET_DOMAIN/./-}-stage-tls'
# ---
# apiVersion: traefik.containo.us/v1alpha1
# kind: Middleware
# metadata:
#   name: proxy-headers
#   namespace: networking
# spec:
#   headers:
#     customRequestHeaders:
#       X-Forwarded-Proto: "https"
# # ref: https://community.traefik.io/t/websockets-not-working-after-migrating-to-traefik-2-0/1912
