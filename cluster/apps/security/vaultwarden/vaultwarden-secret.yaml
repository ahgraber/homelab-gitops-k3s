# yamllint disable
apiVersion: v1
kind: Secret
metadata:
    name: vaultwarden-secret
    namespace: vaultwarden
stringData:
    admin-token: "${VAULTWARDEN_TOKEN}"
    database-url: "mysql://vaultwarden:${DB_USER_PWD}@mariadb-galera.mariadb:3306/vaultwarden"
    smtp-user: "${SMTP_USER}"
    smtp-password: "${SMTP_PWD}"
