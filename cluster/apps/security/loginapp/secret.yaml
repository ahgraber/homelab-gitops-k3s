# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: secret-loginapp
  namespace: security
stringData:
  LOGINAPP_SECRET: "${SECRET_LOGINAPP}"
  LOGINAPP_OIDC_CLIENT_SECRET: "${SECRET_LOGINAPP_OIDC}"
