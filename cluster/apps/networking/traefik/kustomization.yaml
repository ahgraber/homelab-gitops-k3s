apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - dashboard
  - middlewares
  # - templates  # these are for reference, not for use
  - tls-store
  - helm-release.yaml
  - service-monitor.yaml