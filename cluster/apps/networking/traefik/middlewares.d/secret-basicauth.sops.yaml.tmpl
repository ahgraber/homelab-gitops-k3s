---
# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: traefik-basicauth-secret
  namespace: networking
  annotations:
    reloader.stakater.com/match: "true"
data:
  users: |
    ${SECRET_TRAEFIK_BASICAUTH}
