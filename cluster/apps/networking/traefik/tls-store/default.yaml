---
apiVersion: traefik.containo.us/v1alpha1
kind: TLSStore
metadata:
  name: default
  namespace: networking
spec:
  defaultCertificate:
    # secretName: '${SECRET_DOMAIN/./-}-tls'
    secretName: '${SECRET_DOMAIN/.*[[:alpha:]]/}-stage-tls'
