---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: "${SECRET_DOMAIN/./-}"
  namespace: networking
spec:
  secretName: "${SECRET_DOMAIN/./-}-tls"
  secretTemplate:
    annotations:
      # kubed.appscode.com/sync: "${SECRET_DOMAIN/./-}-tls=kubed" # Sync certificate to matching namespaces
      reflector.v1.k8s.emberstack.com/reflection-allowed: "true"
      reflector.v1.k8s.emberstack.com/reflection-allowed-namespaces: "" # Allow all namespaces
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: "${SECRET_DOMAIN}"
  dnsNames:
    - "${SECRET_DOMAIN}"
    - "*.${SECRET_DOMAIN}"
# ---
# # define blank secret to be filled/used by Cert
# apiVersion: v1
# data:
#   ca.crt: ''
#   tls.crt: ''
#   tls.key: ''
# kind: Secret
# metadata:
#   name: ${SECRET_DOMAIN/./-}-tls
#   namespace: networking
#   annotations:
#     kubed.appscode.com/sync: "${SECRET_DOMAIN/./-}-tls=kubed" # Sync certificate to matching namespaces
# type: kubernetes.io/tls
