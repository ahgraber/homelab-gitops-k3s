---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: "${SECRET_DOMAIN/.com/''}"
  namespace: networking
spec:
  secretName: "${SECRET_DOMAIN/.com/''}-tls"
  issuerRef:
    # name: letsencrypt-production
    name: letsencrypt-staging
    kind: ClusterIssuer
  commonName: "${SECRET_DOMAIN}"
  dnsNames:
    - "${SECRET_DOMAIN}"
    - "*.${SECRET_DOMAIN}"
