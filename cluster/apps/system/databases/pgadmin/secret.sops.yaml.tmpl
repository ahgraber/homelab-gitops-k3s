---
# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: pgadmin-secret
  namespace: postgres
  annotations:
    reloader.stakater.com/match: "true"
stringData:
  # email: ${SECRET_ADMIN_EMAIL}
  password: ${SECRET_DEFAULT_PWD}
