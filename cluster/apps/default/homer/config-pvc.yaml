---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homer-config
spec:
  accessModes:
    - ReadWriteOnce
  # storageClassName: local-path
  storageClassName: csi-truenas-nfs
  resources:
    requests:
      storage: 128Mi
