---
apiVersion: v1
kind: Service
metadata:
  name: hammond
  namespace: services
  labels:
    app.kubernetes.io/name: hammond
    app.kubernetes.io/instance: hammond
spec:
  selector:
    app.kubernetes.io/name: hammond
    app.kubernetes.io/instance: hammond
  ports:
    - name: "3000"
      port: 3000
      protocol: TCP
      targetPort: 3000
