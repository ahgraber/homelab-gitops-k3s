# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: secret-vaultwarden
  namespace: vaultwarden
stringData:
  admin-token: "${SECRET_VAULTWARDEN_TOKEN}"
  # database-url: "mysql://vaultwarden:${SECRET_DB_USER_PWD}@mariadb.mariadb:3306/vaultwarden"
  # database-url: "mysql://vaultwarden:${SECRET_DB_USER_PWD}@mariadb-galera.mariadb:3306/vaultwarden"
  database-url: "postgresql://vaultwarden:${SECRET_DB_USER_PWD}@postgresql.postgresql:5432/vaultwarden"
  # database-url: "postgresql://vaultwarden:${SECRET_DB_USER_PWD}@postgresql-ha.postgresql:5432/vaultwarden"
  smtp-user: "${SECRET_SMTP_USER}"
  smtp-password: "${SECRET_SMTP_PWD}"
