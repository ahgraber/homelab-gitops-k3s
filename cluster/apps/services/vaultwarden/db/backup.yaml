---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: vaultwarden-cnpg-backup
  namespace: vaultwarden
spec:
  schedule: "0 0 0 * * *" # midnight
  # schedule: "0 0 */4 * * *" # every 4 hr
  backupOwnerReference: self
  cluster:
    name: vaultwarden
