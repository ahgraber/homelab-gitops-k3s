---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: mealie
  namespace: services
spec:
  interval: 15m
  chart:
    spec:
      # renovate: registryUrl=https://bjw-s.github.io/helm-charts
      chart: app-template
      version: 1.2.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
  install:
    remediation:
      retries: 5
  upgrade:
    remediation:
      retries: 5
  # dependsOn:
  #   - name: mealie-cnpgdb
  #     namespace: postgres
  values:
    global:
      nameOverride: mealie
      annotations:
        reloader.stakater.com/search: "true"

    image:
      repository: docker.io/hkotel/mealie
      tag: frontend-nightly@sha256:270c7b1abb6e73410acea51eaed90dbb5718bbc151623e2cf5555a10184be233
      # tag: frontend-v1.0.0beta-5
    env:
      TZ: ${TIMEZONE}
      API_URL: "mealie.services.svc.cluster.local:9090"
      BASE_URL: "https://mealie.${SECRET_DOMAIN}"
    service:
      main:
        ports:
          http:
            port: 8080
            targetPort: 80

    persistence:
      data:
        enabled: true
        existingClaim: mealie-data
        mountPath: /app/data

    resources:
      requests:
        cpu: 100m
        memory: 128M
      limits:
        memory: 256M

    additionalContainers:
      - name: api
        image: docker.io/hkotel/mealie
        tag: api-nightly@sha256:eaa0636d219f3002af6f09604c56de26a2d755466c8493425cd9b48353906ac0
        # tag: api-v1.0.0beta-5
        env:
          TZ: ${TIMEZONE}
          API_PORT: 9090
          BASE_URL: "https://mealie.${SECRET_DOMAIN}"
        envFrom:
          - secretRef:
              name: mealie-env-secret

        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            memory: 384Mi
