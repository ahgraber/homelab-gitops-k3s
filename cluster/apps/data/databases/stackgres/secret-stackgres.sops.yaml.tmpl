---
# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: stackgres-secret
  namespace: stackgres
  labels:
    api.stackgres.io/auth: user
stringData:
  k8sUsername: admin
  # password="$(echo -n "${USERNAME}${PASSWORD}"| sha256sum | awk '{ print $1 }' )"
  password: < see command above >
  clearPassword: ${SECRET_DEFAULT_PWD}
