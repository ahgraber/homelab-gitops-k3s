---
# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: velero-secret
  namespace: velero
stringData:
  cloud: |
    [default]
    aws_access_key_id=${SECRET_B2_ACCESS_KEY}
    aws_secret_access_key=${SECRET_B2_SECRET_KEY}
  # additional key/value pairs to be used as environment variables such as "DIGITALOCEAN_TOKEN: <your-key>".
---
# ref: https://github.com/vmware-tanzu/velero/issues/2514#issuecomment-630361325
apiVersion: v1
kind: Secret
metadata:
  name: velero-restic-credentials
  namespace: velero
stringData:
  repository-password: ${SECRET_DB_ROOT_PWD}
