# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: secret-mariadb
  namespace: backend
stringData:
  mariadb-root-password: ${SECRET_DB_ROOT_PWD}
  mariadb-replication-password: ${SECRET_DB_ROOT_PWD}
  mariadb-password: ${SECRET_DB_USER_PWD}
