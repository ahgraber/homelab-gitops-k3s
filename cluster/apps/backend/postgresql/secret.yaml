# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: secret-postgresql
  namespace: postgresql
stringData:
  postgresql-username: postgres
  postgresql-postgres-password: ${DB_ROOT_PWD}
  postgresql-password: ${DB_ROOT_PWD}
  postgresql-replication-username: repl_user
  postgresql-replication-password: ${DB_ROOT_PWD}