# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: secret-postgresql
  namespace: postgresql
stringData:
  postgresql-username: postgres
  postgresql-password: ${DB_ROOT_PWD}
  postgresql-postgresPassword: ${DB_ROOT_PWD}
  pgpool-adminUsername: admin
  pgpool-adminPassword: ${DB_ROOT_PWD}