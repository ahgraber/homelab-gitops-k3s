# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: postgresql-secret
  namespace: postgresql
stringData:
  username: postgres
  password: ${DB_ROOT_PWD}
  postgresPassword: ${DB_USER_PWD}
