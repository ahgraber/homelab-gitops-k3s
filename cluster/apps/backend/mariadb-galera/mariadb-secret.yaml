# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: mariadb-secret
  namespace: mariadb
stringData:
  mariadb-root-password: ${DB_ROOT_PWD}
  mariadb-replication-password: ${DB_ROOT_PWD}
  mariadb-password: ${DB_USER_PWD}
  mariadb-galera-mariabackup-password: ${DB_ROOT_PWD}
