---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRepository
metadata:
  name: minio-charts
  namespace: flux-system
spec:
  interval: 1h
  # operator (clustered/multitenancy -- i.e., not what we want!)
  # url: https://operator.min.io/
  # gateway (https://github.com/minio/ming)
  url: https://ming.min.io/
  timeout: 3m
