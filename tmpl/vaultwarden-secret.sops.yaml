# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: vaultwarden-secret
  namespace: vaultwarden
stringData:
  admin-token: ${VAULTWARDEN_TOKEN}
  database-url: ${VAULTWARDEN_DB}
  smtp-user: ${SMTP_USER}
  smtp-password: ${SMTP_PWD}
