# ---
# apiVersion: v1
# kind: Namespace
# metadata:
#   name: mariadb
---
apiVersion: v1
kind: Secret
metadata:
  name: mariadb-secret
  namespace: mariadb
type: Opaque
stringData:
  mariadb-root-password: ${MARIADB_ROOT_PWD}
  mariadb-password: ${MARIADB_PWD}
  mariadb-replication-password: ${MARIADB_REPL_PWD}
  mariadb-galera-mariabackup-password: ${MARIADB_GALERA_BACKUP_PWD}
